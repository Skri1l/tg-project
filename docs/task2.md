Цель задания
В этом домашнем задании вы продолжите разработку Telegram-бота, добавив три новые команды для взаимодействия с пользователями.

Кроме того, вы начнёте реализацию сервиса Scrapper — сервиса, отвечающего за автоматическое отслеживание изменений по ссылкам. В рамках этого задания вы:

Реализуете HTTP-клиенты для внешних и внутренних сетевых вызовов.

Добавите планировщик задач (например, периодическую проверку ссылок).

Настроите получение HTTP-запросов в соответствии с OpenAPI-контрактом.

Данное домашнее задание является продолжением предыдущего. Убедитесь, что каркас бота работает корректно. Цель текущего этапа — расширить функциональность, а не переписывать существующую логику.

Функциональные требования
Бот должен поддерживать следующие команды:
/track — начать отслеживание ссылки. Опционально пользователь может указать один или несколько тегов, привязанных к ссылке.

/untrack — прекратить отслеживание ссылки.

/list — вывести список всех ссылок, отслеживаемых пользователем.
Опционально вторым параметром можно указать тег — в этом случае список ссылок должен быть отфильтрован по указанному тегу.

Команда /list должна выводить специальное сообщение, если список отслеживаемых ссылок пуст.
Неизвестные команды должны игнорироваться с уведомлением пользователю.
При попытке добавить ссылку, которая уже отслеживается, выводится сообщение "Ссылка уже отслеживается"
Все endpoint'ы соответствуют OpenAPI-контракту
Вы можете расширять модель LinkUpdate в Bot API для упрощения форматирования, но основная структура и поля из контракта должны сохраняться.

Ссылка на контракт OpenAPI: https://gist.github.com/sanyarnd/e35dc3d4e0c8000205ec5029dac38f5a (2 файла)

Реализован базовый планировщик: бот присылает простейшее уведомление (заглушку) в случае обнаружения изменений
Для Java -- @Scheduled
для Go -- gocron
для Scala -- Scheduler[ZIO]/quartz4s[CE]
для C# — Quartz.NET
Достаточно факта уведомления, пока нет требования писать с детализацией, например "обновление в комментарии от пользователя Х по ссылке Y". В следующем ДЗ планировщик должен учитывать факт, что пользователь не один, а пока вы можете реализовать самую простую версию
Общение между приложениями bot и scrapper происходит по HTTP
Нефункциональные требования
Функционал фонового отслеживания ссылок должен быть реализован в отдельном сервисе Scrapper, не в сервисе Телеграм-бота.
Токен авторизации должен храниться в конфигурационном файле, недоступном для общего доступа
Используйте только те библиотеки, которые уже включены в предоставленный шаблон проекта. Подключение дополнительных зависимостей запрещено без предварительного согласования в чате курса.
Поддерживается скреппинг вопросов StackOverflow и репозиториев GitHub -- вам нужно написать HTTP-клиентов для получения необходимой информации из API
Для текущей версии задания достаточно факта, что появились обновления, без детализации, что именно было обновлено, т.е. достаточно использовать факт изменения поля, отвечающего за дату последнего изменения (lastUpdated и т.п.)

Не нужно парсить страницу (HTML), нужно работать с API: GitHub, StackOverflow

Запрещается использовать готовые SDK для доступа к API для HTTP-клиентов (GitHub, StackOverflow):
Клиентов нужно написать руками

По возможности используйте декларативные клиенты

Готовые SDK (например, PyGithub, octokit) использовать запрещено.

Бот должен реализовывать машину состояний (создание /track должно идти в формате диалога, а не команды):
неверно: /track tag=work tag=hobby https://foo.bar/baz

верно:

После ввода /track бот получает ссылку на отслеживаемый ресурс.

После получения ссылки бот запрашивает теги (необязательно). Теги -- это слова или короткие фразы, разделённые запятыми, по которым пользователь может классифицировать ресурс.
Примеры: работа, баг, документация, рефакторинг.

Пользователь может ввести /cancel, чтобы прервать процесс.

Если пользователь отправляет другую команду — процесс отслеживания отменяется.

В тестах запрещено делать реальные вызовы к API внешних систем, нужно использовать заглушки (mocks)
Используйте типобезопасную конфигурацию
Пример для Java, Python, Go, Scala, IOptions<T> в .NET

Используйте структурное логирование (добавление key-value значений к логу, вместо зашивания данных в поле message) в коде приложения. Подумайте об этом в конце, когда основной код будет написан.
Бонусные требования
Настройте интеграционные тесты с использованием Testcontainers: (+10 баллов)
Запустите Scrapper и Bot в виде контейнеров
Протестируйте end-to-end взаимодействие через HTTP.
Убедитесь, что тесты не зависят от локального запуска сервисов.
Добавьте возможность взаимодействия между Телеграм-ботом и сервисом Scapper по протоколу gRPC. Преобразование типов разрешено (например, string вместо uuid, int64 для timestamp), но семантика и структура должны сохраняться. (+20 баллов)
Технологии и примеры
Для каждого языка рекомендуется использовать официальные или популярные библиотеки для работы с Telegram Bot API (например, python-telegram-bot, telegram-bot-api для Go и т.д.), они уже включены в шаблон.

Сценарии автотестирования
Покрытие позитивных и негативных тестов
Пользователь отправляет /track и корректную ссылку (например, https://github.com/user/repo), а затем теги и фильтры

Данные сохранены в локальное хранилище

Пользователь отправляет /track и некорректную ссылку (например, tbank://github.com/user/repo).

Бот уведомляет пользователя, что ссылка некорректна

Пользователь в рамках запроса /track отправляет ссылку, на которую уже подписан

Бот уведомляет пользователя, что он уже подписан на эту ссылку

Пользователь отправляет запрос /list, у него есть активные подписки

Бот присылает пользователю список активных подписок

Пользователь отправляет запрос /list, у него нет активных подписок

Бот присылает пользователю сообщение о том, что у него нет активных подписок

Пользователь отправляет запрос /list <tag>, у него есть активные подписки

Бот присылает пользователю список активных подписок, имеющих тег <tag>

Планировщик отправляет обновление только пользователям, которые следят за ссылкой

Другие пользователи не получают обновление

HTTP-клиент (GitHub, StackOverflow) отправляют некорректный ответ сервису Scapper (HTTP-код не 2xx или тело ответа не соответствует заявленной схеме)

Scrapper корректно обрабатывает ошибочные HTTP-статусы и некорректные тела ответов от внешних API (GitHub, StackOverflow), не приводя к падению приложения.

Универсальные сценарии для всех языков (blackbox, контрактное тестирование)
Реализуйте следующие интеграционные тесты:

Тест 1: Корректный запрос к сервису Бота
Отправьте POST-запрос /updates в сервис Бота с телом, соответствующим ожидаемой схеме.
Убедитесь, что ответ — 200 OK.
Тест 2: Некорректный запрос к сервису Бота
Отправьте POST-запрос /updates в сервис Бота с телом, не соответствующим ожидаемой схеме (например, с отсутствующими или неверными полями).
Убедитесь, что ответ — не 200 OK (например, 400 Bad Request).
Тест 3: Работа с сервисом Scrapper
3.1 Добавление и получение ссылки
Отправьте POST-запрос /tg-chat/1 для регистрации Telegram-чата.
→ Ожидается: 200 OK
Отправьте POST-запрос /links с валидным телом, содержащим ссылку и chatId: 1.
→ Ожидается: 200 OK
Отправьте GET-запрос /links.
→ Ожидается: 200 OK, в ответе — список ссылок, содержащий добавленную ранее.
3.2 Добавление и удаление ссылки
Отправьте POST-запрос /tg-chat/1 для регистрации чата.
→ Ожидается: 200 OK
Отправьте POST-запрос /links с chatId: 1.
→ Ожидается: 200 OK
Отправьте DELETE-запрос /links с телом, содержащим ссылку и chatId: 1.
→ Ожидается: 200 OK
Отправьте GET-запрос  /links.
→ Ожидается: 200 OK, в ответе — список ссылок без удалённой.
3.3 Попытка удаления ссылки из несуществующего чата
Отправьте POST-запрос /tg-chat/1 для регистрации чата.
→ Ожидается: 200 OK
Отправьте POST-запрос /links с chatId: 1.
→ Ожидается: 200 OK
Отправьте DELETE-запрос /links с chatId, которого нет в системе (например, chatId: 999).
→ Ожидается: не 200 OK (например, 404 или 400)
Отправьте GET-запрос /links.
→ Ожидается: 200 OK, в ответе — список всё ещё содержит ранее добавленную ссылку.
3.4 Добавление ссылки в несуществующий чат
Отправьте POST-запрос /tg-chat/1 для регистрации чата.
→ Ожидается: 200 OK
Отправьте POST-запрос /links с chatId, которого нет в системе (например, chatId: 2).
→ Ожидается: не 200 OK
3.5 Работа с удалённым чатом
Отправьте POST-запрос /tg-chat/1 для регистрации чата.
→ Ожидается: 200 OK
Отправьте DELETE-запрос /tg-chat/1 для удаления чата.
→ Ожидается: 200 OK
Попробуйте отправить POST-запрос /links с chatId: 1.
→ Ожидается: не 200 OK
3.6 Удаление несуществующего чата
Отправьте DELETE-запрос /tg-chat/1, если чат ещё не был зарегистрирован.
→ Ожидается: 404 Not Found
Пошаговый план реализации
Советы по реализации
Изучите общее описание проекта, чтобы было понятно, какое из приложений за что отвечает: у вас есть 2 приложения, которые общаются между собой по HTTP
Помните, что бот и Scrapper — два отдельных сервиса, а общение между ними происходит по протоколу HTTP (и gRPC для бонуса).
Учтите, что Scrapper — фоновый сервис с планировщиком, который отсылает HTTP-запрос Телеграм-боту в случае обнаружения обновления.
Бот — обрабатывает команды от пользователя, использует машину состояний (state machine).
Не забывайте, что итоговый код должен быть тестируемым: используйте принцип Arrange-Act-Assert
Что не нужно делать в этом домашнем задании?
Работы с БД и системами очередей будет дальше, а пока можно считать, что всё хранится в памяти в контейнерах типа "Словарь" или "Список", для этого используйте шаблон репозиторий.
Не требуется реализовывать функционал парсинга HTML-страниц, нужно работать с API: GitHub, StackOverflow и не используйте готовый SDK этих сервисов.
Не усложняйте архитектуру: очереди сообщений, базы данных, кэширование будут делаться в следующих домашних заданиях :)
Критерии оценки
За выполнение домашнего задания Вы можете получить 100 баллов (+30 дополнительно).

FR 1.1 ( Команда /track: запускает диалог, принимает ссылку, теги, фильтры)

15

FR 1.2 (Команда /untrack: удаляет ссылку из отслеживания)

15

FR 1.3 (Команда /list: показывает список ссылок или сообщение "нет ссылок")

15

NFR: Реализован сервис Scrapper, он отслеживает ссылки, получая запросы по HTTP

30

NFR: Scapper уведомляет ТГ-бот по REST API о получении обновления

15

Написаны тесты к сервисам

10

BR: тесты в TestContainers

10

BR: REST → gRPC

20

Чеклист для MR в GitLab
Перед отправкой MR на проверку убедитесь, что:

Код проходит локальную сборку и запуск

Все токены (для Telegram-бота, Github, Stackoverflow) вынесены в .env или конфиг, а .env добавлен в .gitignore

Все автотесты в Merge Request пройдены

FAQ
API GitHub или StackOverflow отдает мне HTTP-код 429 или другой признак того, что я делаю слишком много запросов. Что мне делать?
Подождите пару часов и доступ восстановится. В будущем, не делайте так много запросов к API: несмотря на то, что оно открытое, владельцы все равно могут Вас заблокировать за большое количество запросов.
